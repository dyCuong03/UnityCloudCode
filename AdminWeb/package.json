{
  "name": "admin-web-mailbox",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "gen:ids": "node scripts/gen-payout-ids.mjs",
    "build": "npm run gen:ids && tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "dev:api": "npx wrangler@3.65.0 pages dev dist --port 8788",
    "dev:full": "npm run build && npx wrangler@3.65.0 pages dev dist --port 8788"
  },
  "devDependencies": {
    "happy-dom": "^13.10.1",
    "typescript": "^5.3.3",
    "vite": "^5.1.0",
    "vitest": "^1.4.0"
  }
}
